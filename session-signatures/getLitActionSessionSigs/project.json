{
    "name": "getLitActionSessionSigs-nodejs",
    "root": "session-signatures/getLitActionSessionSigs",
    "targets": {
        "test-lit": {
            "executor": "nx:run-commands",
            "options": {
                "command": "cd session-signatures/getLitActionSessionSigs && yarn && yarn test"
            }
        },
        "update-lit": {
            "executor": "nx:run-commands",
            "options": {
                "command": "cd session-signatures/getLitActionSessionSigs && LATEST_VERSION=$(npm show @lit-protocol/lit-node-client version) && npm pkg set dependencies.@lit-protocol/lit-node-client=^$LATEST_VERSION"
            }
        }
    }
}