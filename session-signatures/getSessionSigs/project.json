{
    "name": "getSessionSigs-nodejs",
    "root": "session-signatures/getSessionSigs",
    "targets": {
        "test-lit": {
            "executor": "nx:run-commands",
            "options": {
                "command": "cd session-signatures/getSessionSigs && yarn && yarn test"
            }
        },
        "update-lit": {
            "executor": "nx:run-commands",
            "options": {
                "command": "chmod +x ../../scripts/update-lit.sh && ../../scripts/update-lit.sh"
            }
        }
    }
}