{
    "name": "pkp-migration-nodejs",
    "root": "pkp-migration/nodejs",
    "targets": {
        "test-lit": {
            "executor": "nx:run-commands",
            "options": {
                "command": "cd pkp-migration/nodejs && yarn && yarn test"
            }
        },
        "update-lit": {
            "executor": "nx:run-commands",
            "options": {
                "command": "chmod +x ../../scripts/update-lit.sh && ../../scripts/update-lit.sh"
            }
        }
    }
}